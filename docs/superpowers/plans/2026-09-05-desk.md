# Desk (waifu coding) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship Desk slices A–I in the isolated `feat/mcp-client` worktree (`/Users/linux4life/dev/fpai-mcp-client`) so a grenade cannot dirty Rawhide.

**Architecture:** New island under `lib/services/desk/` + `lib/ui/desk/`. No `ChatService` construction, no Realism/Needs/Journal ticks, no `web_ui/`, no OpenCode/Bun/LSP-zoo process. Slice A is chrome + wizard only. Slice B is the in-process `generateWithTools` loop + jail. Later slices add permissions, bash, todos, web/MCP, resume, subagents, opt-in language doors.

**Tech Stack:** Flutter/Dart, Provider, `dart:io` Directory walker, existing `LLMService.generateWithTools`, AppColors / porch amber, Drift only if slice G needs a table (JSON under the data dir is allowed).

**Spec:** `docs/superpowers/specs/2026-09-05-waifu-coding-design.md`

## Global Constraints

- Work **only** in `/Users/linux4life/dev/fpai-mcp-client` (`feat/mcp-client`). Never write, format, or commit `/Users/linux4life/dev/front-porch-AI` (Rawhide).
- Every Dart file under 500 lines. No `lib/` file may reach 1,000.
- TDD: failing test first, watch it fail, then implement. New test files only — do not edit existing tests (test-integrity).
- `HomeModeToggle` constructor must keep `showStories` / `onShowChats` / `onShowStories` so `test/ui/pages/home/home_grid_toolbar_overflow_test.dart` still compiles. Desk is an **additive** optional (`showDesk`, `onShowDesk`).
- AppColors / `formMasterAccent` / `porchAmberOf` / `onChaosAccent`. No `Colors.blueAccent`. No hard-coded `Color(0xFF…)`.
- Barrel imports. `dart format` only files you touched. Never `dart format .`.
- No `web_ui/` Desk page (maintainer deferred). No OpenCode/Bun/ACP spawn. No stdio MCP. No silent LSP zoo.
- Honesty gate copy is blunt (Claude Code / Grok Build / OpenCode; never critical codebases). Advanced audience — no kindergarten copy.
- `FilePicker` is forbidden on the folder step. In-app `Directory.list` only.
- Sync I/O is banned in `lib/ui/**` `build` paths.
- Web parity N/A this conversation. Path-complete chat matrix N/A (separate pipeline).
- Commit each finished slice in this worktree. Do not push. Do not merge to Rawhide.
- Uncommitted MCP Porch Life files already in this worktree are **not** Desk — do not fold them into Desk commits.

---

## File map (slice A)

**Create:**
- `lib/services/desk/desk.dart` — barrel
- `lib/services/desk/desk_honesty.dart` — honesty copy + checkbox label constants
- `lib/services/desk/desk_coworker_prompt.dart` — `buildDeskCoworkerPrompt(CharacterCard)`
- `lib/services/desk/desk_sit_down.dart` — `DeskMode`, `deskCanSitDown(...)`
- `lib/services/desk/desk_folder_listing.dart` — async directory listing + project markers
- `lib/services/desk/desk_session.dart` — in-memory session (folder, coworker, mode, transcript)
- `lib/ui/desk/desk.dart` — barrel
- `lib/ui/desk/desk_home_view.dart` — third home pane + Sit down CTA
- `lib/ui/desk/desk_wizard_page.dart` — 3-step Create-Character chrome
- `lib/ui/desk/desk_wizard_project_step.dart` — folder walker
- `lib/ui/desk/desk_wizard_coworker_step.dart` — character pick
- `lib/ui/desk/desk_wizard_sit_down_step.dart` — recap + honesty + mode
- `lib/ui/desk/desk_page.dart` — empty session chrome (portrait, composer, no loop)
- `test/services/desk/desk_coworker_prompt_test.dart`
- `test/services/desk/desk_sit_down_test.dart`
- `test/services/desk/desk_folder_listing_test.dart`
- `test/ui/desk/desk_home_toggle_test.dart`
- `test/ui/desk/desk_wizard_test.dart`
- `test/ui/desk/desk_session_chrome_test.dart`

**Modify:**
- `lib/ui/pages/home/widgets/home_mode_toggle.dart` — third Desk button; keep old named args
- `lib/ui/pages/home_page.dart` — `_homeMode` enum; Desk pane
- `lib/ui/pages/home/home_page_chrome.dart` — wire Desk callback
- `.claude/changelog.md`, `docs/Rawhide.md`

**Do not modify:** any existing test, `web_ui/`, `ChatService`, `pubspec.yaml`, `database.dart`.

---

### Task 1: Pure prompt + honesty + sit-down gate

**Files:**
- Create: `lib/services/desk/desk_honesty.dart`
- Create: `lib/services/desk/desk_coworker_prompt.dart`
- Create: `lib/services/desk/desk_sit_down.dart`
- Create: `lib/services/desk/desk.dart`
- Test: `test/services/desk/desk_coworker_prompt_test.dart`
- Test: `test/services/desk/desk_sit_down_test.dart`

**Interfaces:**
- Consumes: `CharacterCard` (`name`, `description`, `personality`, `systemPrompt`, `mesExample`, `scenario`, `frontPorchExtensions`)
- Produces:
  - `const kDeskHonestyBody` / `kDeskHonestyCheckbox` / `kDeskPreamble`
  - `String buildDeskCoworkerPrompt(CharacterCard card)`
  - `enum DeskMode { plan, build, yolo }`
  - `bool deskCanSitDown({required bool honestyAccepted, required bool toolsSupported, required bool hasFolder, required bool hasCoworker})`

- [ ] **Step 1: Write the failing tests** (see files below in this plan).
- [ ] **Step 2: Run them — expect missing-symbol / compile fail.**
- [ ] **Step 3: Minimal implementation.**
- [ ] **Step 4: Tests pass.**
- [ ] **Step 5: Commit** `feat(desk): coworker prompt, honesty copy, sit-down gate`

Prompt rules (spec §5): include name, description, personality, systemPrompt if non-empty, truncated mesExample as style hint, plus `kDeskPreamble`. Must **not** contain `scenario`, lorebook text, firstMessage, needs/occupation, or the words of a scenario unique string used in the test (`SCENARIO_MUST_NOT_APPEAR`).

Honesty body must contain: `Claude Code`, `Grok Build`, `OpenCode`, `critical codebase` (or `critical codebase` / `code I cannot afford to lose`). Checkbox label is exactly:

`I understand. I will not use Desk on code I cannot afford to lose.`

`deskCanSitDown` is true only when all four flags are true. Tools-unsupported is a hard block even if the box is ticked.

---

### Task 2: Folder listing (no FilePicker)

**Files:**
- Create: `lib/services/desk/desk_folder_listing.dart`
- Test: `test/services/desk/desk_folder_listing_test.dart`

**Produces:**
```dart
class DeskDirEntry { final String name; final String path; }
class DeskFolderListing {
  final String path;
  final String? parentPath;
  final List<DeskDirEntry> directories;
  final List<String> projectHints; // markers present in this folder
}
const kDeskProjectMarkers = ['pubspec.yaml', '.git', 'package.json', 'Cargo.toml', 'project.godot'];
Future<DeskFolderListing> listDeskDirectory(String path);
String deskDefaultStartPath();
```

- [ ] Failing test: temp dir with a child folder + `pubspec.yaml` → listing includes child, `projectHints` contains `pubspec.yaml`, `parentPath` is the parent.
- [ ] Listing must not throw on a missing path; return empty directories.
- [ ] `deskDefaultStartPath` uses `HOME` / `USERPROFILE` / systemTemp — never a hard-coded `/Users/`.
- [ ] Implement with `Directory.list(followLinks: false)`. Skip `Link` entries.
- [ ] Commit `feat(desk): in-app folder listing`

---

### Task 3: Home toggle + Desk home pane

**Files:**
- Modify: `lib/ui/pages/home/widgets/home_mode_toggle.dart`
- Modify: `lib/ui/pages/home_page.dart`
- Modify: `lib/ui/pages/home/home_page_chrome.dart`
- Create: `lib/ui/desk/desk_home_view.dart`
- Create: `lib/ui/desk/desk.dart`
- Test: `test/ui/desk/desk_home_toggle_test.dart`

Keep constructor:

```dart
const HomeModeToggle({
  super.key,
  required this.showStories,
  required this.onShowChats,
  required this.onShowStories,
  this.showDesk = false,
  this.onShowDesk,
});
```

Always render a third `_ModeButton(label: 'Desk', icon: Icons.desk)`. If `onShowDesk` is null, `onTap` is empty (overflow test still compiles and layout stays icon-safe). Raise `labeledMinWidth` to `400` so three labels drop to icons before the 360px overflow case.

Home state: replace `bool _showStories` with `enum HomeMode { chats, stories, desk }` stored on `_HomePageState` as `_homeMode`. Stories branch unchanged. New Desk branch wraps `DeskHomeView` the same way Stories wraps `StoryHomeView` (toggle bar + status bar). Desk is reachable on an empty character library.

- [ ] Test: `HomeModeToggle` shows `Desk`. Tapping it calls `onShowDesk`.
- [ ] Commit `feat(desk): home mode sibling of Porch Stories`

---

### Task 4: Wizard (Project → Coworker → Sit down)

**Files:**
- Create: `lib/ui/desk/desk_wizard_page.dart`
- Create: `lib/ui/desk/desk_wizard_project_step.dart`
- Create: `lib/ui/desk/desk_wizard_coworker_step.dart`
- Create: `lib/ui/desk/desk_wizard_sit_down_step.dart`
- Create: `lib/services/desk/desk_session.dart`
- Test: `test/ui/desk/desk_wizard_test.dart`

Create-Character chrome: AppBar dots + labels + connecting lines, `AnimatedSwitcher` on `_currentStep` (0 Project, 1 Coworker, 2 Sit down), Back/Next at the bottom. Linear. No side menu.

Inject for tests:
- `List<CharacterCard> characters`
- `bool toolsSupported`
- `bool isLocalBackend`
- `String backendLabel`
- `String initialFolder` (temp dir)
- `void Function(DeskSession session)? onSatDown`

Sit down Confirm (`key: Key('desk-sit-down-confirm')`) is **null** `onPressed` until `deskCanSitDown`. Checkbox `Key('desk-honesty-checkbox')`. Tools-unsupported shows blocking copy, Confirm stays dead even after tick. Local backend shows the small-model warning, not a hard block. Yolo selection shows the jail-still-holds line.

Source of `desk_wizard_project_step.dart` must not contain `FilePicker`.

- [ ] Widget tests covering: step labels, checkbox gate, tools block, FilePicker absent from that file.
- [ ] Commit `feat(desk): sit-down wizard with honesty gate`

---

### Task 5: Empty session chrome

**Files:**
- Create: `lib/ui/desk/desk_page.dart`
- Test: `test/ui/desk/desk_session_chrome_test.dart`

Portrait + coworker name + folder basename + composer. Send appends a user bubble to the in-memory transcript and does **not** call an LLM. No Character State / Journal / Needs / Continue. No `ChatService` import in `lib/ui/desk/` or `lib/services/desk/`.

- [ ] Test finds coworker name, folder name, composer; `grep ChatService lib/services/desk lib/ui/desk` is empty.
- [ ] Commit `feat(desk): empty session chrome (no loop yet)`
- [ ] Update `.claude/changelog.md` + `docs/Rawhide.md`

---

## Slices B–I (do not start until A is committed)

Each later slice gets its own TDD cycle in this worktree. Contracts:

| Slice | Done when | Key types / tools |
|---|---|---|
| **B** | Send loops `generateWithTools`. Tools: read/edit/write/glob/grep. Jail. Max 20. Abort. Work strip. Personality preamble every generate. | `DeskHarness`, `DeskJail.resolve(root, path)`, `DeskLlm` |
| **C** | Plan/Build/Yolo gears. Plan cannot mutate. Build Allow once / Always / Deny. Yolo skips ask. Hard-deny `git checkout --`, restore dirty, `rm -rf /`. Doom-loop 3×. `.env` deny. | `DeskPermissions` |
| **D** | `bash` cwd=root, timeout, clip. Undo/redo of **her** writes only. | `DeskBash`, `DeskUndo` |
| **E** | todos, question, @files, `/init` AGENTS.md, skill loader | `DeskTodos`, `DeskQuestion` |
| **F** | webfetch (no-redirect), FP websearch, opt-in HTTP MCP | existing MCP hub, not stdio |
| **G** | compaction, resume last desk, title | JSON under data dir, not `messages` |
| **H** | Explore (read-only nested) + General nested; child cannot leave jail | after A–G |
| **I** | Language-help catalog, none pre-ticked, PATH first, pin+checksum, custom command, Yolo does not auto-open | after D |

Regen ruling (spec left a pick): **disabled** in v1. Continue control does not exist. A new send is a new loop.

---

## Hostile-review cadence (maintainer `/loop`)

After each slice commit, the next turn: (1) hostile self-review of that commit, (2) fix load-bearing holes, (3) only then start the next slice. Ledger: `.grok/desk-loop/progress.md` (gitignored).
